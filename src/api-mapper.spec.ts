import { async, TestBed } from '@angular/core/testing';

describe('ApiMapper', () => {

  beforeEach(async(() => {

    return TestBed
      .configureTestingModule({
        declarations: []
      })
      .compileComponents();
  }));

  it('TODO', () => {
    // TODO
  });
});
