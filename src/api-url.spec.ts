import { async, TestBed } from '@angular/core/testing';

describe('ApiUrl', () => {

    beforeEach(async(() => {

        return TestBed
            .configureTestingModule({
                declarations: []
            })
            .compileComponents();
    }));

    it('TODO', () => {
        // TODO
    });
});
